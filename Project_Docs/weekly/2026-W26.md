# Week 2026-W26 (06/22 - 06/28)

## Plan
- [ ] Goal 1: Build a real-time interface (GUI) to control the bionic hand.
- [ ] Goal 2: Test the system live and fix any control issues.
- [ ] Goal 3: Write down the issues found during testing and why the current code struggles.

## Done
- [x] Goal 1: Finished `main.py` with PyQtGraph. Can now see the muscle signals live on screen and easily link them to hand movements using drop-down menus.
- [x] Goal 2: Fixed a major bug where the system would freeze because of breadboard noise. Proved that using 5 features (like checking the signal ratio and variance) stops this from happening.
- [ ] Goal 3:

## Key Files Changed
- `Software/GUI_application/main.py` — Built the interface and stopped using fixed numbers to trigger movements. The system now waits until it recognises a clear "Open" gesture before it resets.
- `Software/control_scripts/sEMG_handlers/Random_forest.py` — Updated the training code to handle the 5 new features and the 4 hand gestures.

## Blockers / Questions
- After relaxing a muscle, the signal takes a while to drop back down, and the resting baseline keeps shifting. Current Random Forest model gets confused by these lingering signals.

## Next Week Preview
- Change our data collection code to save the full continuous signal data, rather than just short summaries.
- Wait for the new signal capture device to arrive so can avoid the heavy interference from custom-built setup.
- Try building a simple 1D-CNN (a basic deep learning model) to see if it can automatically handle the drifting and delayed signals.